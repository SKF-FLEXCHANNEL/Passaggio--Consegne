{
  "name": "passaggio-consegne-api",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:remote": "wrangler d1 execute passaggio-consegne-db --file=./schema.sql --remote",
    "db:init:local": "wrangler d1 execute passaggio-consegne-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
